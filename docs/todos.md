# To-dos

One item = one PR. Remove the finished item and renumber when done (see
[context.md](context.md) workflow).

1. Show mastering progress / logs in the UI instead of stdout.
2. Add drag-and-drop for batch mode (drop a folder of tracks onto the app).
3. Package/sign a distributable build (macOS first, per current dev
   environment).
