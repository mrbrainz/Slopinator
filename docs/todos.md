# To-dos

One item = one PR. Remove the finished item and renumber when done (see
[context.md](context.md) workflow).

1. Bundle a real Python runtime + deps (e.g. via PyInstaller) into the
   distributable, instead of relying on system `python3`. Needed before
   handing a build to anyone besides this dev machine — also needs real
   (non-ad-hoc) code signing/notarization for that.
