# To-dos

One item = one PR. Remove the finished item and renumber when done (see
[context.md](context.md) workflow).

1. Wire the UI to `master.py` via a child process (or port the chain to
   Node/WASM if we want to drop the Python dependency).
2. Show mastering progress / logs in the UI instead of stdout.
3. Add drag-and-drop for batch mode (drop a folder of tracks onto the app).
4. Package/sign a distributable build (macOS first, per current dev
   environment).
